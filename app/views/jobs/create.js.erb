// The code below is the javascript to make the link have a diffeent text. When there is a clock to that "#assign" function the following sentence appears.

$("#assign_#{ b.id }").html("<%=b.name%> has been assigned to this job.")